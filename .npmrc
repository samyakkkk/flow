# Refuse to install on an unsupported Node BEFORE any dependency scripts run.
# (The scripts/check-node.mjs preinstall guard alone fires too late on npm 10 —
# dependency install scripts like native builds run first and fail confusingly.)
engine-strict=true
