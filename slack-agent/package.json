{
  "name": "slack-agent",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "description": "Flow Slack agent: Slack agent_view surface (DMs, channels, Slack Connect, group DMs) in front of a swappable answer runtime.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "node --import tsx/esm --test test/slack-agent.test.ts",
    "verify": "npm test"
  },
  "dependencies": {
    "@slack/bolt": "^5.0.0",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
