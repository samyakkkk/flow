{
  "name": "flow",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Flow \u2014 multi-project CLI for the knowledge-graph agent system.",
  "bin": {
    "flow": "bin/flow.mjs"
  },
  "scripts": {
    "dev": "npm run --workspace orchestrator dev & npm run --workspace graph-gateway dev & npm run --workspace dashboard dev",
    "verify": "bash verify-all.sh"
  },
  "workspaces": [
    "orchestrator",
    "graph-gateway",
    "dashboard"
  ],
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
