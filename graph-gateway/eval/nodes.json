[{"id":"api:dashboard:GET /","type":"APIEndpoint","name":"GET / (Home)","description":"Flow Home page (/). States: loading → no-brain (KeyGate, paste OpenRouter key) → empty (source picker: GitHub/Linear/Fireflies/Meeting notes/Slack locked) → bui"},{"id":"api:dashboard:GET /activity","type":"APIEndpoint","name":"GET /activity","description":"Flow Activity page (/activity) — full audit log (every classification, action, suppressed, proposed, approved, dismissed, setting_change, graphwrite, contextblo"},{"id":"api:dashboard:GET /agents","type":"APIEndpoint","name":"GET /agents","description":"Flow Agents page (/agents). Lists connected repos, installed agents (Claude Code/Codex/OpenCode), and active sessions; entry point for kicking off new coding se"},{"id":"api:dashboard:GET /agents/:id","type":"APIEndpoint","name":"GET /agents/:id","description":"Flow /agents/[id] — single session page with the full session panel: metadata, streaming transcript (SSE), collapsible thinking + tool rows, model + mode select"},{"id":"api:dashboard:GET /api/agents","type":"APIEndpoint","name":"GET /api/agents","description":"GET /api/agents — proxy to orchestrator /v1/agents (installed agents + repos)."},{"id":"api:dashboard:GET /api/agents/sessions","type":"APIEndpoint","name":"GET /api/agents/sessions","description":"GET /api/agents/sessions — list. POST /api/agents/sessions {backend, repo, prompt} — create. Server-side proxies to orchestrator."},{"id":"api:dashboard:GET /api/agents/sessions/:id","type":"APIEndpoint","name":"GET /api/agents/sessions/:id","description":"GET /api/agents/sessions/:id — proxy to orchestrator. Returns metadata + slimmed transcript."},{"id":"api:dashboard:GET /api/agents/sessions/:id/events","type":"APIEndpoint","name":"GET /api/agents/sessions/:id/events","description":"GET /api/agents/sessions/:id/events?since=N — pass-through SSE stream from orchestrator. Adds the bearer header server-side because EventSource can't. 502 if up"},{"id":"api:dashboard:GET /api/audit","type":"APIEndpoint","name":"GET /api/audit","description":"GET /api/audit?limit=N. Server-side proxy to orchestrator GET /v1/audit. Powers the Home page \"Recent activity\" feed and the /activity page."},{"id":"api:dashboard:GET /api/auth/check","type":"APIEndpoint","name":"GET /api/auth/check","description":"GET /api/auth/check → {ok: true|false} (200 or 401). Validates the session token by calling orchestrator /v1/audit?limit=1. Called by middleware to gate every p"},{"id":"api:dashboard:GET /api/connections","type":"APIEndpoint","name":"GET /api/connections","description":"GET /api/connections — list which integration keys are set + pending_repos. POST /api/connections {kind} where kind is \"keys\" (store Linear/Slack/Fireflies keys"},{"id":"api:dashboard:GET /api/github/repos","type":"APIEndpoint","name":"GET /api/github/repos","description":"GET /api/github/repos — list the user's GitHub repos via gh CLI or a stored PAT. POST {action: \"save_pat\", pat} — store a GitHub PAT in dashboard localConfig. P"},{"id":"api:dashboard:GET /api/graph/neighborhood","type":"APIEndpoint","name":"GET /api/graph/neighborhood","description":"GET /api/graph/neighborhood?nodeId=&graph= — proxy to graph-gateway POST /v1/verbs/get_entity. Returns the anchor node (cited:true) plus its 1-hop outgoing + in"},{"id":"api:dashboard:GET /api/graph/overview","type":"APIEndpoint","name":"GET /api/graph/overview","description":"GET /api/graph/overview — server-side proxy to graph-gateway POST /v1/verbs/read_query with a safe Cypher (MATCH n, n-[r]->m LIMIT 300). Translates FalkorDB's i"},{"id":"api:dashboard:GET /api/health","type":"APIEndpoint","name":"GET /api/health","description":"GET /api/health → {orchestrator: {ok, latencyMs, detail}, gateway: {ok, latencyMs, detail}}. Probes both services' /health in parallel with a 4s timeout. Used b"},{"id":"api:dashboard:GET /api/ingest/status","type":"APIEndpoint","name":"GET /api/ingest/status","description":"GET /api/ingest/status → {sources: [...]}. Server-side proxy to orchestrator /v1/ingest/status with empty fallback on error. Used by the \"Now indexing\" panel an"},{"id":"api:dashboard:GET /api/jobs/:id","type":"APIEndpoint","name":"GET /api/jobs/:id","description":"GET /api/jobs/:id — proxy to orchestrator. Used by the Ask page to poll for the job result."},{"id":"api:dashboard:GET /api/mode","type":"APIEndpoint","name":"GET /api/mode","description":"GET /api/mode → {mode: local|prod, gates: {slack: prod_only}}. Server-side proxy to orchestrator /v1/mode with graceful fallback to local on orchestrator error."},{"id":"api:dashboard:GET /api/outbox","type":"APIEndpoint","name":"GET /api/outbox","description":"GET /api/outbox?status=pending — server-side proxy to orchestrator /v1/outbox. Powers the \"proposed actions\" inbox."},{"id":"api:dashboard:GET /api/policies","type":"APIEndpoint","name":"GET /api/policies","description":"GET /api/policies — full policy matrix (effective+overrides+defaults). PATCH /api/policies — update overrides. Server-side proxies to orchestrator. Used by Sett"},{"id":"api:dashboard:GET /api/repos","type":"APIEndpoint","name":"GET /api/repos","description":"GET /api/repos — reads the workspace/repos.json registry (REPOS_JSON_PATH or $cwd/../index-workspace/repos.json by default). POST /api/repos {action: \"reindex\","},{"id":"api:dashboard:GET /api/settings","type":"APIEndpoint","name":"GET /api/settings","description":"GET /api/settings — list all settings (masked secrets). PUT /api/settings — bulk update. Server-side proxies to orchestrator. Used by the Settings page."},{"id":"api:dashboard:GET /ask","type":"APIEndpoint","name":"GET /ask","description":"Flow Ask page (/ask). Client form posts to /api/ask with wait:true, shows the answer markdown + citations + confidence + gaps, and renders the cited subgraph vi"},{"id":"api:dashboard:GET /connections","type":"APIEndpoint","name":"GET /connections","description":"Flow Connections page (/connections) — full source/integration management (keys, pending repos, manual meeting notes)."},{"id":"api:dashboard:GET /login","type":"APIEndpoint","name":"GET /login","description":"GET /login — the only unauthenticated page. Form posts the admin token to /api/auth/login. The middleware redirects any other page to /login when /api/auth/chec"},{"id":"api:dashboard:GET /permissions","type":"APIEndpoint","name":"GET /permissions","description":"Flow Permissions page (/permissions) — per-integration permission / scope management."},{"id":"api:dashboard:GET /repos","type":"APIEndpoint","name":"GET /repos","description":"Flow Repos page (/repos) — list, add, reindex connected repos."},{"id":"api:dashboard:GET /settings","type":"APIEndpoint","name":"GET /settings","description":"Flow Settings page (/settings) — manages integrations + per-source toggle (auto/propose/off policies) and integration keys."},{"id":"api:dashboard:POST /api/agents/sessions/:id/:action","type":"APIEndpoint","name":"POST /api/agents/sessions/:id/:action","description":"POST /api/agents/sessions/:id/[action] where action ∈ {prompt, cancel, permission, mode, config, open}. Forwards to the corresponding orchestrator route. Used b"},{"id":"api:dashboard:POST /api/ask","type":"APIEndpoint","name":"POST /api/ask","description":"POST /api/ask {question}. Server-side proxy to orchestrator POST /v1/ask with wait:true (polls up to 30s). Returns the answer (or job id). Used by the floating "},{"id":"api:dashboard:POST /api/auth/login","type":"APIEndpoint","name":"POST /api/auth/login","description":"POST /api/auth/login {token}. Validates the token by hitting orchestrator /v1/audit. On success sets the httpOnly SESSION_COOKIE (\"flow_session\", 30 days, sameS"},{"id":"api:dashboard:POST /api/auth/logout","type":"APIEndpoint","name":"POST /api/auth/logout","description":"POST /api/auth/logout — clears the SESSION_COOKIE. Used by the dashboard to sign out."},{"id":"api:dashboard:POST /api/onboarding/openrouter","type":"APIEndpoint","name":"POST /api/onboarding/openrouter","description":"POST /api/onboarding/openrouter {key}. Validates the key against OpenRouter /api/v1/key first (no 401/403 = valid), then PUTs it through orchestrator /v1/settin"},{"id":"api:dashboard:POST /api/outbox/approve","type":"APIEndpoint","name":"POST /api/outbox/approve","description":"POST /api/outbox/approve {id, decision: \"approve\"|\"dismiss\"} — server-side proxy to orchestrator PATCH /v1/outbox/:id."},{"id":"api:graph-gateway:GET /health","type":"APIEndpoint","name":"GET /health","description":"Liveness probe. Returns 200 {ok:true, verbs:[...]} on success; used by docker-compose healthcheck and the indexer's preflight."},{"id":"api:graph-gateway:GET /v1/journal","type":"APIEndpoint","name":"GET /v1/journal","description":"Tail the append-only journal of writes. Returns the last `limit` entries (default 50) as {entries: [{ts, graph, actor, session, verb, input, status}, ...]}. Use"},{"id":"api:graph-gateway:POST /v1/verbs/find_entity","type":"APIEndpoint","name":"POST /v1/verbs/find_entity","description":"Typed verb HTTP surface of the graph-gateway. POST /v1/verbs/<name> with the verb's input JSON; returns {status, ...}. 7 verbs: find_entity, upsert_entity, upse"},{"id":"api:graph-gateway:POST /v1/verbs/get_entity","type":"APIEndpoint","name":"POST /v1/verbs/get_entity","description":"Fetch a single node by id with all its incoming and outgoing relationships (type, target id/name, edge props). Returns status:\"not_found\" if missing."},{"id":"api:graph-gateway:POST /v1/verbs/list_schema","type":"APIEndpoint","name":"POST /v1/verbs/list_schema","description":"Returns the whitelist of allowed node types and edge types. Used by the indexer to know what it can write."},{"id":"api:graph-gateway:POST /v1/verbs/merge_entities","type":"APIEndpoint","name":"POST /v1/verbs/merge_entities","description":"Merge two nodes that turned out to be the same real-world thing. Rewires all edges from `remove` to `keep`, fills props `keep` lacks, folds the removed id+name "},{"id":"api:graph-gateway:POST /v1/verbs/read_query","type":"APIEndpoint","name":"POST /v1/verbs/read_query","description":"Read-only Cypher escape hatch. Rejects queries containing create/merge/set/delete/remove/drop/call keywords. Use only for traversals the typed verbs don't cover"},{"id":"api:graph-gateway:POST /v1/verbs/upsert_entity","type":"APIEndpoint","name":"POST /v1/verbs/upsert_entity","description":"Create or update a graph node. Requires provenance (actor + optional evidence + optional confidence). If the id exists, updates name/props and stamps updated_by"},{"id":"api:graph-gateway:POST /v1/verbs/upsert_relation","type":"APIEndpoint","name":"POST /v1/verbs/upsert_relation","description":"Create or update a typed edge between two existing nodes. Validates edge type against the schema whitelist, verifies both endpoints exist (returns \"Missing node"},{"id":"api:orchestrator:GET /health","type":"APIEndpoint","name":"GET /health (orchestrator)","description":"GET /health (unauthenticated) — returns {status, service, port, ts}. Used by docker-compose healthcheck, the dashboard's /api/health probe, the CLI's waitForHea"},{"id":"api:orchestrator:GET /v1/agents","type":"APIEndpoint","name":"GET /v1/agents","description":"GET /v1/agents → {agents: [{id, name, installed, version?, installHint}], repos: [{name, path, cloned}]}. Detects installed Claude Code / Codex / OpenCode (cach"},{"id":"api:orchestrator:GET /v1/agents/sessions","type":"APIEndpoint","name":"GET /v1/agents/sessions","description":"GET /v1/agents/sessions → list of agent_sessions rows newest first (up to 100), each augmented with `live: boolean` and the current status if a LiveSession exis"},{"id":"api:orchestrator:GET /v1/agents/sessions/:id","type":"APIEndpoint","name":"GET /v1/agents/sessions/:id","description":"GET /v1/agents/sessions/:id → metadata row + (when live) modes, configOptions, pendingPermissions, and the full transcript (events slimmed for tool_call rows). "},{"id":"api:orchestrator:GET /v1/agents/sessions/:id/events","type":"APIEndpoint","name":"GET /v1/agents/sessions/:id/events","description":"GET /v1/agents/sessions/:id/events?since=seq — Server-Sent Events stream. Sends full transcript replay (filtered by since if given) then live updates as the ses"},{"id":"api:orchestrator:GET /v1/audit","type":"APIEndpoint","name":"GET /v1/audit","description":"GET /v1/audit?limit=N (max 500). Returns the audit_log rows newest first. Every classification, action, suppressed/proposed/outbox_approve/outbox_dismiss, setti"},{"id":"api:orchestrator:GET /v1/config/policies","type":"APIEndpoint","name":"GET /v1/config/policies","description":"GET /v1/config/policies → {effective, overrides, defaults}. effective = defaults merged with overrides from config table; \"sensitive\" is always off (hardcoded, "},{"id":"api:orchestrator:GET /v1/corpus/search","type":"APIEndpoint","name":"GET /v1/corpus/search","description":"GET /v1/corpus/search?q=&source=&limit=. Full-text search across the corpus (slack_messages_fts, linear_tickets_fts, meeting_segments_fts). source filters to on"},{"id":"api:orchestrator:GET /v1/events/:id","type":"APIEndpoint","name":"GET /v1/events/:id","description":"GET /v1/events/:id — returns the event row (with payload parsed back to JSON) plus the matching audit_log actions for that event. 404 if unknown."},{"id":"api:orchestrator:GET /v1/ingest/status","type":"APIEndpoint","name":"GET /v1/ingest/status","description":"GET /v1/ingest/status → {sources: [{source, resource, cursor, last_poll_at, lag_s, lag_seconds, catching_up, status}]}. status ∈ ok | error | catching_up | disa"},{"id":"api:orchestrator:GET /v1/jobs/:id","type":"APIEndpoint","name":"GET /v1/jobs/:id","description":"GET /v1/jobs/:id — returns the full job row (id, type, input, status, result_json, repo, notify_count, session_id, created_at, updated_at) with result parsed ba"},{"id":"api:orchestrator:GET /v1/llmlog","type":"APIEndpoint","name":"GET /v1/llmlog","description":"GET /v1/llmlog?kind=&ref=&limit= (max 200). Returns the llm_log rows newest first, optionally filtered by kind (classifier|opencode_job) and/or ref (event_id or"},{"id":"api:orchestrator:GET /v1/mode","type":"APIEndpoint","name":"GET /v1/mode","description":"GET /v1/mode → {mode: \"local\" | \"prod\", gates: {slack: \"prod_only\"}}. FLOW_MODE env var; defaults to \"local\" (Slack disabled — laptop can't be always-on)."},{"id":"api:orchestrator:GET /v1/outbox","type":"APIEndpoint","name":"GET /v1/outbox","description":"GET /v1/outbox?status=pending (default). Returns up to 200 outbox rows newest first. Outbox is the queue of proposed/drained side effects: slack_post, dm, linea"},{"id":"api:orchestrator:GET /v1/settings","type":"APIEndpoint","name":"GET /v1/settings","description":"GET /v1/settings → list of all settings with {key, secret, description, appliesTo, source (db|env|default|unset), value (masked for secrets), set}. Source prece"},{"id":"api:orchestrator:PATCH /v1/config/policies","type":"APIEndpoint","name":"PATCH /v1/config/policies","description":"PATCH /v1/config/policies with body {key: auto|propose|off}. Merges into the overrides stored in the config table. 400 on invalid value. The dashboard Settings "},{"id":"api:orchestrator:PATCH /v1/outbox/:id","type":"APIEndpoint","name":"PATCH /v1/outbox/:id","description":"PATCH /v1/outbox/:id {decision: \"approve\" | \"dismiss\"}. Approve = mark row \"approved\" + replay the original event in auto mode through executeAction(); dismiss "},{"id":"api:orchestrator:POST /v1/agents/graph-activity","type":"APIEndpoint","name":"POST /v1/agents/graph-activity","description":"POST /v1/agents/graph-activity {session, verb, args, nodeIds, ok}. Called by the read-only flow-graph MCP subprocess (loopback) on every tool call. The orchestr"},{"id":"api:orchestrator:POST /v1/agents/sessions","type":"APIEndpoint","name":"POST /v1/agents/sessions","description":"POST /v1/agents/sessions {backend: claude|codex|opencode, repo, prompt}. Spawns the matching ACP adapter subprocess (claude-agent-acp / codex-acp / opencode acp"},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/cancel","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/cancel","description":"POST /v1/agents/sessions/:id/cancel — stop the current turn. Clears the steering queue, resolves every pending permission as \"cancelled\" (per ACP spec), and set"},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/config","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/config","description":"POST /v1/agents/sessions/:id/config {configId, value}. Set a session config option — most importantly the model selector. Surfaces the model picker + effort/thi"},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/mode","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/mode","description":"POST /v1/agents/sessions/:id/mode {modeId} — set the session's mode (per the modes the agent advertised on session create)."},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/open","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/open","description":"POST /v1/agents/sessions/:id/open {target: \"finder\" | \"vscode\"}. Local-mode convenience: open the session's repo checkout in the OS file manager or VS Code. Spa"},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/permission","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/permission","description":"POST /v1/agents/sessions/:id/permission {requestId, optionId|null}. Resolves a pending permission card the agent is waiting on. Auto-approval already happens fo"},{"id":"api:orchestrator:POST /v1/agents/sessions/:id/prompt","type":"APIEndpoint","name":"POST /v1/agents/sessions/:id/prompt","description":"POST /v1/agents/sessions/:id/prompt {text}. \"Steer\" the session. When idle, starts a new turn immediately. When a turn is active, queues the text and cancels th"},{"id":"api:orchestrator:POST /v1/ask","type":"APIEndpoint","name":"POST /v1/ask","description":"POST /v1/ask {question, wait?}. Enqueues an `answer` job (the opencode answerer agent is dispatched with question as the prompt). If wait:true, polls the job ro"},{"id":"api:orchestrator:POST /v1/events","type":"APIEndpoint","name":"POST /v1/events","description":"POST /v1/events — accepts a NormalizedEvent {id?, source: slack|linear|github|meeting|dashboard, type, ts, payload, workspace?}. Server runs the deterministic p"},{"id":"api:orchestrator:POST /v1/meetings/upload","type":"APIEndpoint","name":"POST /v1/meetings/upload","description":"POST /v1/meetings/upload {title, date?, transcript}. Splits the transcript on speaker turns, DROPS any segment matching a secret pattern (before insert), insert"},{"id":"api:orchestrator:POST /v1/notify","type":"APIEndpoint","name":"POST /v1/notify","description":"POST /v1/notify {job_id, text}. Receives notifications from opencode notify tool. Auth = admin bearer OR a job-scoped HMAC token (so a leaked session token only"},{"id":"api:orchestrator:POST /v1/webhooks/github","type":"APIEndpoint","name":"POST /v1/webhooks/github","description":"POST /v1/webhooks/github — receives GitHub push events. Validates X-Hub-Signature-256 when GITHUB_WEBHOOK_SECRET set. Only emits when the branch matches a regis"},{"id":"api:orchestrator:POST /v1/webhooks/linear","type":"APIEndpoint","name":"POST /v1/webhooks/linear","description":"POST /v1/webhooks/linear — receives Linear Issue create/update events. Validates HMAC-SHA256 of body using LINEAR_WEBHOOK_SECRET (header linear-signature or x-l"},{"id":"api:orchestrator:PUT /v1/settings","type":"APIEndpoint","name":"PUT /v1/settings","description":"PUT /v1/settings with a body of {KEY: value}. Validates keys against the registry. Secrets AES-256-GCM encrypted at rest. Empty value deletes the DB override (r"},{"id":"cap:graph-gateway.merge-entities","type":"Capability","name":"cap:graph-gateway.merge-entities","description":"Capability: merge two nodes that turned out to be the same real-world thing (rewires all edges, merges props, deletes the duplicate)."},{"id":"cap:graph-gateway.read-consult","type":"Capability","name":"cap:graph-gateway.read-consult (4 tools, read-only)","description":"Capability: read-only graph consultation. Tools: find_entity, get_entity, read_query, list_schema. What every coding-agent session gets."},{"id":"cap:graph-gateway.upsert-entity","type":"Capability","name":"cap:graph-gateway.upsert-entity","description":"Capability: create or update a graph node through the gateway, with provenance + dedup gate."},{"id":"cap:graph-gateway.upsert-relation","type":"Capability","name":"cap:graph-gateway.upsert-relation","description":"Capability: create or update a typed edge through the gateway, with provenance."},{"id":"cap:index-workspace.index-repo","type":"Capability","name":"cap:index-workspace.index-repo (graph-builder)","description":"Capability: discover and persist the service / API / resource ontology of a repository into the graph. Run by the opencode `graph-builder` agent (the indexer is"},{"id":"cap:orchestrator.answer-question","type":"Capability","name":"cap:orchestrator.answer-question (answerer)","description":"Capability: answer a question with citations + confidence + gaps. Run by the opencode `answerer` agent via the orchestrator's answer (and continue) jobs. Uses t"},{"id":"cap:orchestrator.classify-event","type":"Capability","name":"cap:orchestrator.classify-event (LLM classifier)","description":"Capability: classify a NormalizedEvent into a per-source enum (slack_ambient.noise | slack_ambient.knowledge_claim | slack_ambient.correction | slack_ambient.ta"},{"id":"cap:orchestrator.policy-route","type":"Capability","name":"cap:orchestrator.policy-route (auto|propose|off)","description":"Capability: route a classified event to auto | propose | off based on the per-source/per-classification policy matrix. Sensitive is hardcoded off (not configura"},{"id":"concept:agents-produce-service-performs","type":"Concept","name":"Agents produce, service performs side effects","description":"Concept: two hard boundaries. 1) Agents produce content; the service performs side effects. OpenCode sessions get repo-read + graph_* (read-only integration too"},{"id":"concept:flow-mode","type":"Concept","name":"FLOW_MODE (local vs prod)","description":"Concept: Flow's two deployment modes. Local (default, FLOW_MODE=local): build/enrich/ask + polling integrations on a laptop; Slack disabled (a laptop can't be a"},{"id":"concept:flow-project","type":"Concept","name":"Flow project (multi-project model)","description":"Concept: a Flow project. A self-contained bundle of {graph-gateway, orchestrator, dashboard} on its own port triplet, bound to one GRAPH_NAME + one flow.db + on"},{"id":"concept:graph-ontology","type":"Concept","name":"Graph ontology (15 node + 19 edge types)","description":"Concept: the graph ontology the gateway enforces. Whitelist of 15 node types: Service, Repository, APIEndpoint, Handler, Capability, UsageContract, Database, Da"},{"id":"concept:poll-since-cursor","type":"Concept","name":"Poll-since-cursor ingestion (webhook = poll-now accelerator)","description":"Concept: every non-Slack source polls since a stored cursor (poll_cursors), producing NormalizedEvents into the one pipeline. Catch-up after downtime is free — "},{"id":"concept:single-write-path","type":"Concept","name":"Single write path (graph-gateway)","description":"Concept: all graph mutations go through graph-gateway verbs. Typed, schema-validated (node/edge type whitelist enforced before Cypher interpolation), provenance"},{"id":"concept:three-tier-storage","type":"Concept","name":"Three-tier storage model","description":"Concept: a Flow claim lives in exactly one of three storage tiers. 1) FalkorDB graph (distilled durable knowledge: services, capabilities, usage contracts, conc"},{"id":"pg:flow-db","type":"Database","name":"flow.db (orchestrator SQLite)","description":"SQLite file per project. Lives at $PROJECT_DIR/flow.db (when run via `flow up`); defaults to orchestrator/data/flow.db in dev. WAL mode, foreign_keys=ON. Owns e"},{"id":"ddb:flow-db.agent_sessions","type":"DatabaseTable","name":"flow.db.agent_sessions","description":"ACP coding-agent session table. id (PK), backend (claude|codex|opencode), repo, cwd, title, status, acp_session_id, stop_reason, error, created_at, updated_at. "},{"id":"ddb:flow-db.audit_log","type":"DatabaseTable","name":"flow.db.audit_log","description":"One row per classification + per action. event_id (nullable for setting_change), classification, confidence, action (graphwrite, contextblock, propose, suppress"},{"id":"ddb:flow-db.config","type":"DatabaseTable","name":"flow.db.config","description":"Key/value store. key: \"policies\" → JSON {&lt;source&gt;.&lt;classification&gt;: auto|propose|off} merged with defaults. key: \"setting:&lt;NAME&gt;\" → JSON-encod"},{"id":"ddb:flow-db.events","type":"DatabaseTable","name":"flow.db.events","description":"NormalizedEvent table. id (PK), source (slack|linear|github|meeting|dashboard), type, ts (ms), payload (JSON), workspace, classification_json (the persisted Cla"},{"id":"ddb:flow-db.jobs","type":"DatabaseTable","name":"flow.db.jobs","description":"Job queue. id (PK), type (index_repo|enrich|answer|continue), input (JSON), status (queued|running|done|failed), result_json, repo (per-repo mutex key), notify_"},{"id":"ddb:flow-db.linear_tickets","type":"DatabaseTable","name":"flow.db.linear_tickets (+ _fts)","description":"linear_tickets (id, identifier, title, description, state, assignee, labels (JSON), url, updated_at, inserted_at) + linear_tickets_fts. Mirrored from Linear by "},{"id":"ddb:flow-db.llm_log","type":"DatabaseTable","name":"flow.db.llm_log","description":"One row per live LLM interaction. kind (classifier|opencode_job), ref (event_id|job_id), model, attempt, ok (1|0), latency_ms, error, prompt (capped 16k chars),"},{"id":"ddb:flow-db.meeting_segments","type":"DatabaseTable","name":"flow.db.meeting_segments (+ _fts)","description":"meeting_segments (id, meeting_id, speaker, text, start_ms, end_ms, inserted_at) + meeting_segments_fts. Filled by /v1/meetings/upload (manual) and the Fireflies"},{"id":"ddb:flow-db.outbox","type":"DatabaseTable","name":"flow.db.outbox","description":"Outbox for side effects. id, event_id, action_type (slack_post|dm|linear_comment|linear_ticket_create|linear_context_update), payload (JSON), status (pending|se"},{"id":"ddb:flow-db.poll_cursors","type":"DatabaseTable","name":"flow.db.poll_cursors","description":"Per-(source, resource) ingestion cursors. (source, resource) PK; resource defaults to \"_all\" for source-level pollers (linear, fireflies), per-repo for github. "},{"id":"ddb:flow-db.slack_messages","type":"DatabaseTable","name":"flow.db.slack_messages (+ _fts)","description":"slack_messages (id, workspace, channel, user_id, text, ts, thread_ts, permalink, inserted_at) + slack_messages_fts (FTS5 virtual table on text, channel, user_id"},{"id":"ddb:flow-db.thread_sessions","type":"DatabaseTable","name":"flow.db.thread_sessions (G10)","description":"thread_key (PK = \"workspace:channel:thread_ts\") → {session_id, job_id, status (active|dormant), last_activity}. The SESSION-PER-CHAT model: once a thread is bou"},{"id":"ext:acp-adapters","type":"ExternalService","name":"ACP adapters (Claude Code / Codex / OpenCode)","description":"External Agent Client Protocol adapters installed via Flow's npm dependencies. The orchestrator spawns one shared subprocess per backend (claude-agent-acp, code"},{"id":"ext:fireflies","type":"ExternalService","name":"Fireflies.ai","description":"External Fireflies.ai. Flow polls for new meeting transcripts via GraphQL at https://api.fireflies.ai/graphql using FIREFLIES_API_KEY (bearer). No webhook suppo"},{"id":"ext:github","type":"ExternalService","name":"GitHub","description":"External GitHub. Flow uses GITHUB_TOKEN (or legacy GITHUB_PAT) bearer for the REST API (listPRs, getPR, ls-remote HEAD) and for the private-repo clone URL injec"},{"id":"ext:linear","type":"ExternalService","name":"Linear","description":"External Linear workspace. Flow talks to Linear via GraphQL at https://api.linear.app/graphql using LINEAR_API_KEY (bearer) and receives webhooks at /v1/webhook"},{"id":"ext:openrouter","type":"ExternalService","name":"OpenRouter","description":"External OpenRouter LLM gateway. Flow's LiveClassifier posts chat/completions to https://openrouter.ai/api/v1/chat/completions with response_format={type:json_o"},{"id":"ext:slack","type":"ExternalService","name":"Slack","description":"External Slack workspace. Flow talks to Slack via the Web API (chat.postMessage, auth.test, comment helpers via @slack/web-api) and Socket Mode for inbound (app"},{"id":"handler:dashboard:local-config","type":"Handler","name":"dashboard local-config (encrypted keys)","description":"Dashboard-local encrypted store for integration keys (linear_key, slack_bot_token, slack_app_token, fireflies_key) and pending_repos. AES-256-GCM with key = scr"},{"id":"handler:dashboard:middleware-auth","type":"Handler","name":"dashboard middleware (auth gate)","description":"Next.js middleware that gates every non-public page. Calls /api/auth/check (which validates against this project's orchestrator); 401/403 → redirect to /login a"},{"id":"handler:graph-gateway:journal","type":"Handler","name":"graph-gateway append-only journal","description":"Append-only mutation journal that every gateway write is recorded to (ts, graph, actor, session, verb, input, status). Backs the read-model observers use. Path:"},{"id":"handler:graph-gateway:mcp-adapter","type":"Handler","name":"graph-gateway MCP adapter (stdio)","description":"Stdio MCP face of the gateway (npm run mcp). Wraps the same 7 verbs as MCP tools. Two modes: full (default) for Flow's graph builders; read-only (GATEWAY_MCP_RE"},{"id":"handler:graph-gateway:mcp-readonly","type":"Handler","name":"graph-gateway MCP server (read-only mode)","description":"When GATEWAY_MCP_READONLY=1, the same gateway process exposes a 4-tool MCP server (find_entity, get_entity, read_query, list_schema) under the name \"flow-graph\""},{"id":"handler:graph-gateway:opencode-plugin","type":"Handler","name":"graph-gateway opencode plugin (graph_*)","description":"Pre-built opencode plugin file (graph-gateway/opencode-tools/graph.ts) that defines graph_find/graph_upsert/graph_relate/graph_get/graph_read/graph_schema tools"},{"id":"handler:index-workspace:scripts","type":"Handler","name":"index-workspace scripts (add-repo.mjs, update.mjs)","description":"The two scripts that drive the indexer. add-repo.mjs: clone a repo prod branch, register it, kick off a full-index job (calls opencode graph-builder with the sa"},{"id":"handler:orchestrator:acp-runtime","type":"Handler","name":"orchestrator ACP agent runtime","description":"The orchestrator's ACP (Agent Client Protocol) runtime. One shared subprocess per backend (claude-agent-acp, codex-acp, opencode acp). Each new session: ensureC"},{"id":"handler:orchestrator:agent-sessions-dir","type":"Handler","name":"orchestrator agent-sessions transcript dir","description":"Per-agent-session JSONL transcript. Append-only seq-tagged events (created, status, update, user_prompt, permission_request, permission_result, graph, error). T"},{"id":"handler:orchestrator:bearer-auth","type":"Handler","name":"orchestrator bearer auth (requireAuth)","description":"Bearer-token middleware on every orchestrator route except: GET /health, /v1/webhooks/* (HMAC-validated inside each adapter), and /v1/notify (which also accepts"},{"id":"handler:orchestrator:event-pipeline","type":"Handler","name":"orchestrator event pipeline (classify → policy → action)","description":"Deterministic event pipeline run by processEvent(): secret scan (BEFORE persist, hard-drops sk-…/xoxb-…/github_pat_…/lin_api_…/AKIA…/PEM/…), insert events row, "},{"id":"handler:orchestrator:job-logs","type":"Handler","name":"orchestrator opencode job logs (per-job transcripts)","description":"Per-job opencode transcripts on disk. &lt;jobId&gt;.jsonl = the full --format json stream (persisted BEFORE any error handling so failed runs are debuggable). &"},{"id":"handler:orchestrator:job-queue","type":"Handler","name":"orchestrator opencode job queue","description":"Opencode job queue. Types: index_repo (enqueue from dashboard.repo_added/reindex_request and from github_merge.index_worthy), enrich, answer (enqueue from /v1/a"},{"id":"handler:orchestrator:outbox-drainer","type":"Handler","name":"orchestrator outbox drainer","description":"Outbox drainer loop. Polls the outbox table every FLOW_DRAIN_MS (default 3000ms). For each pending row, dispatches by action_type: slack_post|dm → Slack chat.po"},{"id":"handler:orchestrator:pollers-engine","type":"Handler","name":"orchestrator pollers engine","description":"Poll-since-cursor ingestion engine. Register a poller with {source, resource, intervalMs, fetchSince, enabled}; startAllPollers schedules them with ±10% jitter."},{"id":"handler:orchestrator:repos-json","type":"Handler","name":"workspace/repos.json (repo registry)","description":"The workspace's repo registry. JSON file: {repos: [{name, url, branch, lastIndexedCommit, addedAt, lastIndexedAt}]}. Path: $OPENCODE_WORKSPACE_DIR/repos.json (d"},{"id":"handler:orchestrator:settings-registry","type":"Handler","name":"orchestrator settings registry (AES-256-GCM)","description":"Settings registry + AES-256-GCM encrypted store. 13 settings: OPENROUTER_API_KEY (secret), CLASSIFIER_MODEL, GRAPH_BUILDER_MODEL, LINEAR_API_KEY (secret), FIREF"},{"id":"handler:orchestrator:simulators","type":"Handler","name":"simulators (run-scenarios + slack-sim + linear-mock)","description":"Integration-test harness. run-scenarios.ts: boots a fresh orchestrator + linear-mock + gateway-stub in temp dirs, replays every JSON scenario under simulators/s"},{"id":"handler:orchestrator:sqlite-db","type":"Handler","name":"orchestrator SQLite (flow.db)","description":"SQLite database (better-sqlite3) backing the orchestrator. WAL mode, foreign_keys=ON. DB_PATH env (default $PROJECT/data/flow.db). One file per project — projec"},{"id":"repo:flow","type":"Repository","name":"flow","description":"Flow monorepo — a self-hostable knowledge graph + agent runner for codebases. Contains the CLI, graph-gateway (typed verb API over FalkorDB), orchestrator (even"},{"id":"svc:dashboard","type":"Service","name":"dashboard","description":"Flow's Next.js 16 UI (port 7600). Token-gated via httpOnly cookie (FLOW_ADMIN_TOKEN). Talks to the orchestrator for everything stateful (ask bar, agent session "},{"id":"svc:falkordb","type":"Service","name":"FalkorDB","description":"Graph database backing Flow. Stores named graphs (one per project) for the durable service/capability/contract ontology. Shared across all projects on a single "},{"id":"svc:flow-cli","type":"Service","name":"flow CLI","description":"The `flow` command (bin/flow.mjs) installed via `npm install -g .` Multi-project operator CLI: `flow project create` (provisions a project folder + docker-compo"},{"id":"svc:graph-gateway","type":"Service","name":"graph-gateway","description":"Single write path to the memory graph. Exposes a typed verb HTTP API (find_entity, upsert_entity, upsert_relation, get_entity, read_query, merge_entities, list_"},{"id":"svc:index-workspace","type":"Service","name":"index-workspace","description":"The \"graph-builder\" environment that connects repos and indexes them into the graph. Runs `add-repo.mjs` (clone a repo prod branch, register it, full-index) and"},{"id":"svc:opencode","type":"Service","name":"opencode CLI (external binary)","description":"External opencode CLI (https://opencode.ai). The orchestrator spawns it as `opencode run --format json --agent &lt;graph-builder|answerer&gt; --session &lt;id&g"},{"id":"svc:orchestrator","type":"Service","name":"orchestrator","description":"Flow's pipeline + service runtime. Receives NormalizedEvents (Slack/Linear/GitHub/meeting/dashboard, plus pollers), runs an LLM classifier (strict-JSON, per-sou"},{"id":"contract:acp-session->graph-gateway","type":"UsageContract","name":"coding-agent session → graph-gateway (read-only MCP) contract","description":"The coding-agent session's contract with the graph-gateway (read-only by construction). The orchestrator injects a flowGraphMcp(sid) MCP server into every ACP s"},{"id":"contract:dashboard->graph-gateway","type":"UsageContract","name":"dashboard → graph-gateway read contract","description":"The dashboard's read-only contract for the graph-gateway. Server-side proxy at /api/graph/overview (LIMIT 300 nodes, no auth required) and /api/graph/neighborho"},{"id":"contract:dashboard->orchestrator","type":"UsageContract","name":"dashboard → orchestrator HTTP contract","description":"The dashboard's contract for the orchestrator. Authentication is the FLOW_ADMIN_TOKEN, sourced from FLOW_ADMIN_TOKEN env in local mode (auto-auth, no cookie) or"},{"id":"contract:graph-gateway->falkordb","type":"UsageContract","name":"graph-gateway → FalkorDB (Redis protocol) contract","description":"The graph-gateway's only contract with the underlying graph store. Connects to $FALKOR_HOST:$FALKOR_PORT (default localhost:6379) using the falkordb npm client "},{"id":"contract:orchestrator->acp-adapters","type":"UsageContract","name":"orchestrator → ACP adapters contract (Claude/Codex/OpenCode)","description":"The orchestrator's contract for the Agent Client Protocol (ACP) adapters: claude-agent-acp, codex-acp, and `opencode acp`. Spawns one shared subprocess per back"},{"id":"contract:orchestrator->fireflies","type":"UsageContract","name":"orchestrator ↔ Fireflies contract","description":"The orchestrator's contract with Fireflies.ai. Single direction: poll-since-cursor poller (FLOW_FIREFLIES_POLL_MS default 5min) for new meeting transcripts sinc"},{"id":"contract:orchestrator->github","type":"UsageContract","name":"orchestrator ↔ GitHub contract","description":"The orchestrator's contract with GitHub. Two halves: 1) IN — GitHub webhooks (X-Hub-Signature-256, GITHUB_WEBHOOK_SECRET) for push events on watched branches + "},{"id":"contract:orchestrator->graph-gateway","type":"UsageContract","name":"orchestrator → graph-gateway write contract","description":"The orchestrator's write contract for the graph-gateway. Every graph write from the orchestrator (slack knowledge_claim → Concept node, meeting decision/knowled"},{"id":"contract:orchestrator->linear","type":"UsageContract","name":"orchestrator ↔ Linear contract","description":"The orchestrator's contract with Linear. Two halves: 1) IN — Linear webhooks (HMAC-SHA256 of body, LINEAR_WEBHOOK_SECRET) at POST /v1/webhooks/linear + a poll-s"},{"id":"contract:orchestrator->opencode","type":"UsageContract","name":"orchestrator → opencode CLI (subprocess) contract","description":"The orchestrator's contract for the opencode CLI (subprocess). Spawns `opencode run --format json` with env: ORCHESTRATOR_URL, GRAPH_GATEWAY_URL (project-specif"},{"id":"contract:orchestrator->openrouter","type":"UsageContract","name":"orchestrator → OpenRouter LLM contract","description":"The orchestrator's contract for the OpenRouter LLM API. Used by LiveClassifier for the event classification call (POST https://openrouter.ai/api/v1/chat/complet"},{"id":"contract:orchestrator->slack","type":"UsageContract","name":"orchestrator ↔ Slack contract","description":"The orchestrator's contract with Slack. Two halves: 1) IN — Bolt Socket Mode app_mention + ambient message events (PROD MODE ONLY; local mode is locked at boot "},{"id":"workflow:agent-session-graph-highlight","type":"Workflow","name":"Workflow: agent consults brain → dashboard live highlight","description":"Major lifecycle: a coding agent consults the brain and the dashboard lights up. 1) User kicks off /v1/agents/sessions {backend, repo, prompt} → 2) orchestrator "},{"id":"workflow:ask-answer","type":"Workflow","name":"Workflow: ask → answer (dashboard or Slack thread)","description":"Major lifecycle: ask a question, get an answer with citations + confidence + gaps. 1) Dashboard /api/ask {question, wait:true} → 2) orchestrator POST /v1/ask → "},{"id":"workflow:event-pipeline","type":"Workflow","name":"Workflow: event ingest → classify → policy → action","description":"Major lifecycle: every NormalizedEvent runs through the pipeline. Slack thread binding short-circuit (G10) or dashboard.repo_added short-circuit; otherwise secr"},{"id":"workflow:index-repo","type":"Workflow","name":"Workflow: index_repo (connect repo → build graph)","description":"Major lifecycle: connect a repo and watch the graph grow. 1) Dashboard /api/connections {kind:repo} OR /api/github/repos {action:add_repos} OR add-repo.mjs (ind"},{"id":"workflow:outbox-drain","type":"Workflow","name":"Workflow: outbox drain (pending side-effect → external call)","description":"Major lifecycle: side effects to Slack/Linear drain in the background. 1) Action layer writes to outbox (event_id, action_type, payload, status=pending) — never"},{"id":"workflow:project-create-up","type":"Workflow","name":"Workflow: flow project create → flow up → first connect","description":"Major lifecycle: `flow project create <name>` then `flow up <name>`. 1) bin/flow.mjs creates $PROJECT_DIR with project.json, .env (24-byte hex FLOW_ADMIN_TOKEN)"},{"id":"workflow:session-per-chat","type":"Workflow","name":"Workflow: session-per-chat (G10 Slack thread → opencode session)","description":"Major lifecycle: a Slack thread becomes one continuous coding-agent session. 1) Slack app_mention or follow-up ambient message → orchestrator events.ts → 2) com"},{"id":"workflow:settings-hot-reload","type":"Workflow","name":"Workflow: settings update hot-reload (poller reinit + Slack reboot)","description":"Major lifecycle: update an integration key and watch the system pick it up without a restart. 1) Dashboard Settings UI → 2) PUT /api/settings → 3) orchestrator "}]